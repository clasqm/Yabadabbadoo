sub NewProjectFromTemplate()
	local dothis$, cance$
	dothis$ = "yab " + ProgramDir$ + "/yabTemplatePicker.yab " + ProjectPath$
	cancel$ = system$(dothis$)
	if cancel$="cancelled" then 
		MakenewProject()
	else
		OpenProject("fromtemplate")
	endif
end sub

